const AuthFooter = () => (
  <footer className="text-center text-xs text-gray-400 mt-6">
    <p>© {new Date().getFullYear()} Senior-Junior Connect — Built with care.</p>
  </footer>
);

export default AuthFooter;
